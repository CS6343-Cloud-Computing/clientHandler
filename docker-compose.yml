version: "3.9"
services:
    redis:
       image: 'redis:alpine'
       ports:
            - '6379:6379'
    nodeserver:
        build:
            context: ./nodeManager
        ports:
            - "5000:5000"
    nginx:
        build:
            context: ./nginx
        ports:
            - "80:80"
        depends_on:
            - nodeserver
